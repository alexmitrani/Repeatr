% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_fugazidb_data.R
\name{export_fugazidb_data}
\alias{export_fugazidb_data}
\title{Exports fugazi.db's data/*.rda objects from Repeatr's own cleaned data}
\usage{
export_fugazidb_data(fugazidb_dir, repeatr_data_dir = NULL)
}
\arguments{
\item{fugazidb_dir}{Path to a local \code{fugazi.db} checkout. Required - there
is no default, so a caller's own local path is never hardcoded here.}

\item{repeatr_data_dir}{Optional directory to read Repeatr's own already-
saved \verb{data/*.rda} objects from. If omitted, defaults to \verb{data/} under the
current working directory (the package root, after a
\code{\link{Repeatr_Updatr}} run).}
}
\value{
Invisibly, a named list of the six objects written. As a side
effect, writes \verb{fugazidb_dir/data/*.rda}.
}
\description{
Composes fugazi.db's six published tables (\code{shows},
\code{locations}, \code{durations}, \code{discography}, \code{songs}, \code{bands}) from Repeatr's
own already-saved \verb{data/*.rda} objects (the "Derived-cleaned" tier
produced by \code{\link{Repeatr_1}}) and from
\code{inst/extdata/fls_venue_geocoding_v2.csv}/\code{fls_doorprice_currency_lookup.csv}
directly - almost no re-derivation or new business logic, with two small,
documented exceptions confined to \code{shows$subdivision}: Brazilian state
codes are filled in (Repeatr's own scrape never populates a subdivision
outside the US/Canada/Australia) from a hand-verified city lookup, and any
remaining blank subdivision (a pre-existing mix of \code{NA} and \code{""}) is
standardized to \code{NA}. Also runs basic integrity checks (no missing/
duplicate id columns) on each table just before it's written, aborting the
export if any fail. Writes each table as a \verb{data/*.rda} (lazy-loadable)
file directly into a local \code{fugazi.db} checkout. Does not commit or push
anything in that checkout - review and commit fugazi.db's changes
separately.

Excludes anything joined/summarized/modeled (e.g. \code{xray},
\code{duration_summary}, \code{Repeatr1}, and everything from \code{\link{Repeatr_2}}
onward) and the copyrighted free-text show notes (\code{fls_notes}, scraped
from the Fugazi Live Series site - see fugazi.db's own \code{LICENSE}) - see
\code{vignette("Data-Provenance")} for the full tier catalogue.
}
\examples{
\dontrun{
export_fugazidb_data(fugazidb_dir = "../fugazi.db")
}

}
