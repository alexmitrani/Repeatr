% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recap.R
\name{recap}
\alias{recap}
\title{recap brings together all the notable facts about a single Fugazi show: date, venue, tour context, how many times the band had previously played in that country/state/city/venue, the previous and next show of the tour, and (if a recording exists) a detailed tracklist with duration, release and rendition statistics.}
\usage{
recap(
  mygid,
  myshows_data = NULL,
  myduration_data_da = NULL,
  myrepeatr1 = NULL,
  myreleasesdatalookup = NULL,
  myduration_summary = NULL,
  myposition_summary = NULL,
  myplayed_with = NULL,
  myothervariables = NULL,
  mytransitions_data_da = NULL,
  myfls_tags = NULL
)
}
\arguments{
\item{mygid}{gig id of the show to recap, as a string, for instance "washington-dc-usa-13196".}

\item{myshows_data}{optional \code{shows_data} dataframe (as produced by \code{Repeatr_1()}) to be used for show/tour/location details. If omitted the currently lazy-loaded default will be used.}

\item{myduration_data_da}{optional \code{duration_data_da} dataframe (as produced by \code{Repeatr_1()}) to be used for recorded renditions, live durations and set position. If omitted the currently lazy-loaded default will be used.}

\item{myrepeatr1}{optional \code{Repeatr1} dataframe (as produced by \code{Repeatr_1()}) to be used for release/track lookups. If omitted the currently lazy-loaded default will be used.}

\item{myreleasesdatalookup}{optional \code{releasesdatalookup} dataframe (as produced by \code{Repeatr_1()}) to be used for release dates. If omitted the currently lazy-loaded default will be used.}

\item{myduration_summary}{optional \code{duration_summary} dataframe (as produced by \code{Repeatr_1()}) to be used for each song's maximum recorded duration and total number of recorded renditions. If omitted the currently lazy-loaded default will be used.}

\item{myposition_summary}{optional \code{position_summary} dataframe (as produced by \code{Repeatr_1()}) to be used for each song's average recorded set position. If omitted the currently lazy-loaded default will be used.}

\item{myplayed_with}{optional \code{played_with} dataframe (as produced by \code{Repeatr_1()}) to be used for the bands Fugazi played with at the show. If omitted the currently lazy-loaded default will be used.}

\item{myothervariables}{optional \code{othervariables} dataframe (as produced by \code{Repeatr_1()}) to be used for recording credits (recorded by/mastered by/original source). If omitted the currently lazy-loaded default will be used.}

\item{mytransitions_data_da}{optional \code{transitions_data_da} dataframe (as produced by \code{Repeatr_1()}) to be used for song-to-song transition occurrence counts. If omitted the currently lazy-loaded default will be used.}

\item{myfls_tags}{optional \code{fls_tags} dataframe (as produced by \code{Repeatr_1()}) to be used for the raw per-track tagged duration of non-song tracks (interludes, intro/outro, etc.), which have no duration elsewhere. If omitted the currently lazy-loaded default will be used.}
}
\value{
A list of three elements: \code{context} (a named list of the show's prose-summary facts, including ready-made \code{paragraph1}/\code{paragraph2} strings), \code{tracklist} (a dataframe with one row per track on the recording, songs and non-song tracks alike, or \code{NULL} if no recording exists) and \code{release_breakdown} (a dataframe of song counts by release for this show, or \code{NULL} if no recording exists).
}
\description{
recap brings together all the notable facts about a single Fugazi show: date, venue, tour context, how many times the band had previously played in that country/state/city/venue, the previous and next show of the tour, and (if a recording exists) a detailed tracklist with duration, release and rendition statistics.
}
\examples{
result <- recap(mygid = "washington-dc-usa-13196")
result$context$where_played
result$tracklist

}
