% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Repeatr_5.R
\name{Repeatr_5}
\alias{Repeatr_5}
\title{produces results using a coefficient table for a choice model estimated with mlogit.}
\usage{
Repeatr_5(
  mymodeldf = NULL,
  mysongidlookup = NULL,
  myaltlookup = NULL,
  myfugazi_song_performance_intensity = NULL,
  mysongvarslookup = NULL,
  myreleasesdatalookup = NULL,
  myreleases_data_input = NULL,
  input_dir = NULL,
  output_dir = NULL
)
}
\arguments{
\item{mymodeldf}{optional choice model coefficients dataframe to be used to generate the results. If omitted, the default choice model coefficients dataframe will be used, which is results_ml_Repeatr4.}

\item{mysongidlookup}{optional \code{songidlookup} dataframe (the \code{songidlookup} element of \code{Repeatr_1()}'s return list). If omitted the currently lazy-loaded default will be used.}

\item{myaltlookup}{optional \code{altlookup} dataframe (the second element of \code{Repeatr_2()}'s return list) used to translate \code{mymodeldf}'s \code{alt}-indexed intercept coefficients back to \code{songid}/\code{title}. If omitted the currently lazy-loaded default will be used.}

\item{myfugazi_song_performance_intensity}{optional \code{fugazi_song_performance_intensity} dataframe (the third element of \code{Repeatr_2()}'s return list). If omitted the currently lazy-loaded default will be used - pass this explicitly when calling \code{Repeatr_5()} right after a fresh \code{Repeatr_2()} in the same session, since the lazy-loaded default reflects the last build on disk, not the one just computed.}

\item{mysongvarslookup}{optional \code{songvarslookup} dataframe (the \code{songvarslookup} element of \code{Repeatr_1()}'s return list). If omitted the currently lazy-loaded default will be used.}

\item{myreleasesdatalookup}{optional \code{releasesdatalookup} dataframe (the \code{releasesdatalookup} element of \code{Repeatr_1()}'s return list). If omitted the currently lazy-loaded default will be used.}

\item{myreleases_data_input}{optional \code{releases_data_input} dataframe (the \code{releases_data_input} element of \code{Repeatr_1()}'s return list). If omitted the currently lazy-loaded default will be used.}

\item{input_dir}{Optional directory to write the \code{fugazi_song_choice_model.csv}/\code{fugazi_song_preferences.csv}/\code{releases_rated.csv}/\code{summary.csv} output-export CSVs into. If omitted, defaults to this package's own \code{inst/extdata} (these are Repeatr's own downloadable outputs, not primary/raw data).}

\item{output_dir}{Optional directory to save the rebuilt \verb{data/*.rda} objects into. If omitted, defaults to \verb{data/} under the current working directory.}
}
\value{
A list of 5 elements: \code{fugazi_song_choice_model} (per-variable coefficient table with song names substituted in for the intercept terms), \code{fugazi_song_preferences} (songs ranked by estimated preference), \code{summary} (song performance summary combining counts, intensity and rating), \code{releases_rated} (average rating by release), and \code{releases_data_input} (per-song-per-release data enriched with the estimated rating). Each of these, plus \code{releases_summary}, is also saved into \verb{data/}.
}
\description{
Produces a summary table that includes song performance counts, song performance intensities, and ratings based on the estimated choice model parameters.
}
\examples{
Repeatr_5_results <- Repeatr_5(mymodeldf = results_ml_Repeatr4,
                                output_dir = tempdir(), input_dir = tempdir())

}
