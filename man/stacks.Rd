% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stacks.R
\name{stacks}
\alias{stacks}
\title{stacks puts together a set of shows that will contain a specified number of unique songs.}
\usage{
stacks(
  mydf = NULL,
  mygid = NULL,
  mynumberofsongs = NULL,
  exclude_poor_sound_quality = FALSE,
  mysummary = NULL,
  myothervariables = NULL,
  mygidsoundquality = NULL
)
}
\arguments{
\item{mydf}{dataframs of shows and songs containing the columns gid and title.}

\item{mygid}{gig id of initial show as a string, for instance "washington-dc-usa-13196".}

\item{mynumberofsongs}{the number of unique songs that are required. the maximum is 92 (the number of songs Fugazi played live at least twice and that feature in the studio discography) and the number of songs in the initial show will be taken as a minimum.}

\item{exclude_poor_sound_quality}{set to TRUE to exclude shows with poor sound quality}

\item{mysummary}{optional \code{summary} dataframe (as produced by \code{Repeatr_5()}) to be used for song play counts. If omitted the currently lazy-loaded default will be used - pass this explicitly if calling \code{stacks()} right after a fresh \code{Repeatr_Updatr()} run in the same session.}

\item{myothervariables}{optional \code{othervariables} dataframe (as produced by \code{Repeatr_1()}) to be used for show details. If omitted the currently lazy-loaded default will be used.}

\item{mygidsoundquality}{optional \code{gid_sound_quality} dataframe (as produced by \code{Repeatr_1()}) to be used for sound quality filtering/display. If omitted the currently lazy-loaded default will be used.}
}
\value{
A list of two data frames: \code{stack_songs} (\code{gid}, \code{title} - one row per unique song in the stack, and the show it came from) and \code{stack_shows_songs} (one row per show included in the stack, with venue/date/sound-quality details and the number of new songs it contributed).
}
\description{
stacks puts together a set of shows that will contain a specified number of unique songs.
}
\examples{
gid_song <- duration_data_da \%>\%
  select(gid, title)

results <- stacks(mydf = gid_song, mygid = "washington-dc-usa-13196", mynumberofsongs = 94)
stack1 <- results[[1]]
stack2 <- results[[2]]


}
