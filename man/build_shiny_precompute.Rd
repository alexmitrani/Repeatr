% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_shiny_precompute.R
\name{build_shiny_precompute}
\alias{build_shiny_precompute}
\title{Precompute Fugazetteer's build-time-only Shiny inputs}
\usage{
build_shiny_precompute(output_dir = NULL)
}
\arguments{
\item{output_dir}{where to save the rebuilt \verb{data/*.rda} objects. If
omitted, defaults to \verb{data/} under the current working directory.}
}
\value{
Invisibly, \code{NULL}. The real effect is writing the \code{shiny_*.rda}
files.
}
\description{
Builds and saves the handful of \verb{data/*.rda} objects that
exist purely so \code{inst/shiny/Fugazetteer/app.R} doesn't have to redo, on
every app start, joins/aggregations that never touch the app's three
live Google Sheets reads (\code{fls_venue_geocoding}, \code{quizdata},
\code{linktracksindexdata}). These are Shiny-presentation-only cached views,
not part of Repeatr's canonical data model and not exported to
fugazibase - see \code{vignette("Data-Provenance")} and the \code{Provenance}
section on each object's own help page (e.g. \code{?shiny_shows_data_base}).
Mirrors the exact joins \code{app.R} used to run inline; if \code{app.R}'s
sheet-independent preprocessing ever changes, update both together.
}
\examples{
\dontrun{
build_shiny_precompute()
}
}
