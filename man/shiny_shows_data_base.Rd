% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{shiny_shows_data_base}
\alias{shiny_shows_data_base}
\title{\code{shows_data} enriched with average tempo (BPM), before the live venue-coordinate join}
\format{
dataframe with the same rows as \code{\link{shows_data}}, plus \code{tempo_bpm}.
}
\usage{
shiny_shows_data_base
}
\description{
The base app.R builds its own runtime \code{shows_data} from: joins in the
live \code{fls_venue_geocoding} sheet for coordinates, disambiguates
same-named cities, and de-duplicates by \code{gid}. Coordinates change if the
sheet is edited, so that part must stay a live, runtime join - this
object is everything upstream of it.
}
\section{Provenance}{
 Shiny-presentation-only. Produced by \code{\link{build_shiny_precompute}} from \code{\link{shows_data}}, \code{\link{duration_data_da}}, and \code{\link{song_tempo_bpm_data}}. Not to be confused with the package's own \code{shows_data}, which this does not overwrite.
}

\examples{
shiny_shows_data_base
}
\keyword{datasets}
